[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    integration: integration tests touching multiple modules
    negative: negative / error path tests
    slow: slow tests (guardrails, full lifecycle)
